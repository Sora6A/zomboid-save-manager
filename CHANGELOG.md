# 版本记录

本记录依据现存源码、交付包和故障处理记录整理。历史条目说明代码改动，不代表每个版本都完成了 Windows / 游戏实测；未确认的历史发布日期不作补写。

## 仓库整理（2026-09-13）

- 以保存完整的 1.1.8 源码包为基线；工作副本中被截断的 GUI 文件不进入发布内容。
- 整理 README、安装使用说明、架构说明、排错指南和验证记录；示例使用通用路径。
- 添加忽略规则、Windows 批处理换行规则、故障反馈模板和跨平台 CI。
- 测试夹具显式关闭 SQLite 连接，避免 Windows 清理临时目录时遇到测试自身的文件锁。
- 应用功能版本仍为 1.1.8，本次未增加游戏功能或宣称新的游戏版本兼容性。

## 1.1.8

- SQLite 在线备份显式关闭源连接和目标连接，然后再替换临时文件。
- 针对 Windows `[WinError 32]` 增加连接未关闭时拒绝重命名的回归测试。

## 1.1.7

- 构建脚本校验虚拟环境与 pip；缺失时尝试 `ensurepip`，失败后重建环境。
- 使用 `python -m PyInstaller` 构建。

## 1.1.6

- BK 按钮横向偏移调整到 58，减少与原生左侧工具栏重叠。
- 显式设置按钮启用、可见并尝试置顶。

## 1.1.5

- 独立后台线程监听 Lua 指令，GUI 队列负责处理。
- 增加伴生程序状态文件、玩家检测重试与世界名称回退读取。
- 当时对“按钮未创建”的判断后来被用户发现的“按钮重叠”修正；重试逻辑仍保留。

## 1.1.4

- 增加仅修复快捷方式脚本和不依赖桌面快捷方式的保护启动脚本。
- 创建英文备用快捷方式并检查实际桌面目录。

## 1.1.3

- 恢复根目录 `mod.info + media`，同时保留 `common + 42` 布局。
- 增加 Lua 重复加载保护。

## 1.1.2

- 尝试版本化 Mod 布局，增加载入诊断、按钮重试和仅修复模组脚本。
- 此布局曾导致用户的本地模组不再显示，随后在 1.1.3 调整；不推荐以此版本安装。

## 1.1.1

- Windows 脚本在缺少 `py.exe` 时回退到 `python`。

## 1.1.0

- 加入游戏内 Lua 桥接、BK / F8 / F9、Windows 安装与 Steam 保护启动流程。

## 1.0.0

- 独立 GUI、多槽位备份、定时自动槽、恢复前保险槽和 SQLite 快照基础功能。
